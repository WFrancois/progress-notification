{% extends 'components/base.html.twig' %}

{% block content %}
    <div class="container">
        <h1 class="page-header">Login</h1>
        <a href="{{ oauth.getUrl() }}">Login</a>
        {% if error %}
            <div class="alert alert-danger">
                {{ error }}
            </div>
        {% endif %}
    </div>
{% endblock %}